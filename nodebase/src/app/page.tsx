import React from 'react';
import { caller } from '@/trpc/server';
import { useTRPC } from '@/trpc/client';

const Page = async () => {

  return (
    
    <div>
      Hello
    </div>
  );
};

export default Page;
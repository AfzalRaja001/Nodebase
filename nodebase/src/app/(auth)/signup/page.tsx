import { RegistrationForm } from "@/features/auth/components/registration-form";

const Page = () => {
    return (
        <div>
            <RegistrationForm/>
        </div>
    );
};

export default Page;